import 'package:flutter/material.dart';

class MakeCakeInfoScreen extends StatelessWidget {
  const MakeCakeInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
